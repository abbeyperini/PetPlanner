<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'/>
        <meta name='viewport' content='width=device-width' initial-scale='1.0' maximum-scale='1.0'/>
        <title>PetPlanner</title>
        <link rel="shortcut icon" href="/images/pepper.svg" type="image/x-icon">
        <link rel='stylesheet' href='/styles/styles.css'/>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
    </head>
    <body>
        {{> header}}
        {{> menu}}
        <main>
            <nav>
                <form method="Post" action="/dashboard/add-pet">
                    <input type="text" placeholder="Pet Name" name="petName">
                    <input type="text" placeholder="Image URL" name="image">
                    <button>Submit</button>
                </form>
            </nav>
        </main>
        <aside>
            {{#pets}}
                <h3>{{petName}}</h3>
                <img src="{{image}}" alt="pet photo" width="300px">
            {{/pets}}
        </aside>
        {{> footer}}
    </body>
</html>