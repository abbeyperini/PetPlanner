<!DOCTYPE html>
<html lang='en'>
    <head>
        <meta charset='UTF-8'/>
        <meta name='viewport' content='width=device-width' initial-scale='1.0' maximum-scale='1.0'/>
        <title>PetPlanner</title>
        <link rel="shortcut icon" href="/images/pepper.svg" type="image/x-icon">
        <link rel='stylesheet' href='/styles/styles.css'/>
        <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,400;0,500;0,700;1,400;1,500;1,700&display=swap" rel="stylesheet">
    </head>
    <body>
        {{> header}}
        <main>
            <h3>{{message}}</h3>
            <nav class="loginBox">
                <form method="POST" action="/index/login">
                    <input type="text" placeholder="username" name="username" pattern="[^a-zA-Z \-]|( )|(\-\-)|(^\s*$)">
                    <!--This match fileds contain only letters, a single hyphen, a single space, not empty-->
                    <input type="text" placeholder="password" name="password" pattern="^(?=.*\d).{4,8}$">
                    <!--between 4 and 8 digits long and include at least one numeric digit.-->
                    <button>Login</button>
                </form>
                <p>Need a username? <a href='/index/register'>Register</a></p>
            </nav>
        </main>
    </body>
</html>